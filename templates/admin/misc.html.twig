{% extends 'base.html.twig' %}

{% block title %}Divers{% endblock %}

{% block body %}
    <div class="container my-4">
        <h1 class="h4 mb-3">Divers</h1>
        <div class="row g-3">
            <div class="col-md-4">
                <div class="card h-100">
                    <div class="card-body d-flex flex-column">
                        <h2 class="h6">🧹 Rafraîchir les vignettes</h2>
                        <p class="text-muted small">Recrée les URLs de vignettes en cache pour éviter les liens /resolve.</p>
                        <a class="btn btn-outline-primary mt-auto" href="{{ path('admin_thumbnails_refresh') }}">Ouvrir</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
