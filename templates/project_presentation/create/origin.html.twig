{% extends 'base.html.twig' %}

{% block headTag %}<meta name="robots" content="noindex, nofollow" />{% endblock %}

{% block title %}Aide à la présentation de projet{% endblock %}





{% block body %}

{# ordering steps #}

{% set helperItems = [ 
            
    { 'type': "goal"},
            
    { 'type': "textDescription"},

    { 'type': "imageSlide"},

    { 'type': "title"}, 
    
] %}


{# Setting positions (note: variable position wich is current position, is provided by controller) #}

{% set nextPosition = null %}

    {% if helperItems[position + 1] is defined %}

        {% set nextPosition = position + 1 %}

    {% endif %}

{% set previousPosition = null %}

    {% if helperItems[position - 1] is defined %}

        {% set previousPosition = position - 1 %}

    {% endif %}

{# Form part name that we are displaying #}

{% set currentItem = helperItems[position] %}

{# Progress bar compute #}

{% set itemsCount = helperItems | length %}

{% set progressionPercentage = ( (position + 1) / itemsCount ) * 100 %}

{# Modal with form structure including form parts inside #}
{% include "project_presentation/create/_modal_box_full_screen.html.twig" %} 

{% endblock %}
