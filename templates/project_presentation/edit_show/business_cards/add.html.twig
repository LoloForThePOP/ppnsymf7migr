{% embed "utilities/modal_skeleton.html.twig" with {'modal_id': "add_business_card_form_modal", 'modal_size' : "modal-lg"} %}

    {% block modalTitle %}
    
        🖐️ Comment vous contacter ?
    
    {% endblock %}
        
    {% block modalBody %}

        {{ form_start(addBusinessCardForm, {
            action: path('pp_add_business_card', { stringId: presentation.stringId }),
            method: 'POST'
        }) }}

        {% set businessCardForm = addBusinessCardForm %}
        {% include "project_presentation/edit_show/business_cards/form.html.twig" %}

    {% endblock %}

    {% block footerButtons %}

        <button type="submit" class="btn btn-validate">Valider</button>

        {{ form_end(addBusinessCardForm) }}

    {% endblock %}


    {% block formErrors %}
        
        {% if not addBusinessCardForm.vars.valid %}

            <script>
            
                $(document).ready(function(){
            
                    $('#{{modal_id}}').modal('show');
            
                });
            
            </script>

        {% endif %}
    
    {% endblock %}

{% endembed %}
